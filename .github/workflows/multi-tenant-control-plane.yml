name: Multi-Tenant Control Plane CI

on:
  push:
    paths:
      - 'design-enterprise-solutions/multi-tenant-control-plane/**'
      - '.github/workflows/multi-tenant-control-plane.yml'
  pull_request:
    paths:
      - 'design-enterprise-solutions/multi-tenant-control-plane/**'

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          python-version: '3.10'
          enable-cache: true

      - name: Install Dependencies
        run: make install

      - name: Run Pytest
        run: make test-control
